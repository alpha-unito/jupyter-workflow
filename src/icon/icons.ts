import { LabIcon } from '@jupyterlab/ui-components';

import restartRunWorkflow from '../../style/icons/shuffle.svg';

export const restartRunWorkflowIcon = new LabIcon({
  name: 'jupyter-workflow:restart-run-workflow ',
  svgstr: restartRunWorkflow
});
